<a id="oxen.config"></a>

# oxen.config

<a id="oxen.config.is_configured"></a>

## is\_configured

```python
def is_configured()
```

Checks if the user and auth is configured.

**Returns**:

- ``bool`` - True if the user and auth is configured, False otherwise.

