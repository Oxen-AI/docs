<a id="oxen.diff/text_diff"></a>

# oxen.diff/text\_diff

<a id="oxen.diff/text_diff.TextDiff"></a>

## TextDiff Objects

```python
class TextDiff()
```

A class representing a text diff.

<a id="oxen.diff/text_diff.TextDiff.num_added"></a>

## num\_added

```python
@property
def num_added() -> int
```

Returns the number of added lines in the diff.

<a id="oxen.diff/text_diff.TextDiff.num_removed"></a>

## num\_removed

```python
@property
def num_removed() -> int
```

Returns the number of removed lines in the diff.

<a id="oxen.diff/text_diff.TextDiff.lines"></a>

## lines

```python
@property
def lines() -> list[LineDiff]
```

Returns the contents of the diff as a polars data frame.

