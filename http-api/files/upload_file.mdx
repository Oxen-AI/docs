---
title: "Upload File"
description: "Upload and commit a file to a repository. Resource must specify a branch and commit, or will fallback to an initial upload helper in empty repositories"
openapi: "PUT /api/repos/{namespace}/{repo_name}/file/{resource}"
---