---
title: "Index Data Frame"
description: "Index a data frame into a workspace so that it can be queried with SQL and nearest neighbor search."
openapi: "PUT /api/repos/{namespace}/{repo_name}/workspaces/{workspace_id}/data_frames/resource/{path}"
---

