---
title: "Create Embeddings Index"
description: "Create an embeddings index in a workspace so that it can be used for nearest neighbor search."
openapi: "POST /api/repos/{namespace}/{repo_name}/workspaces/{workspace_id}/embeddings/{path}"
---

