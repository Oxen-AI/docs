---
title: "Update Repository"
openapi: "PUT /api/repos/{namespace}/{repo_name}"
---

Update repository information. Note this only works on the hosted Oxen Hub at `hub.oxen.ai` and not the self-hosted version.
